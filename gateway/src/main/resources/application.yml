
spring:
  application:
    name: gateway
  main:
    web-application-type: reactive
server:
  port: 8080

jwt:
  header: Authorization
  secret: bookingcar

eureka:
  client:
    serviceUrl.defaultZone: "http://127.0.0.1:8761/eureka/"
    enabled: "true"

logging:
  level:
    ROOT: DEBUG